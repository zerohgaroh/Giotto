{
  "name": "giotto",
  "version": "0.1.0",
  "private": true,
  "sideEffects": [
    "**/*.css"
  ],
  "scripts": {
    "clean": "node scripts/clean.cjs",
    "dev": "concurrently -k \"npm:dev:css\" \"npm:dev:server\"",
    "dev:server": "cross-env NODE_ENV=development tsx watch server.js",
    "dev:css": "tailwindcss -i ./src/styles/main.css -o ./public/assets/app.css --watch",
    "db:bootstrap": "node scripts/bootstrap-db.cjs",
    "db:deploy": "node scripts/bootstrap-db.cjs",
    "db:push": "node scripts/prisma-cli.cjs db push",
    "db:seed": "node scripts/run-seed.cjs",
    "db:prepare": "npm run db:deploy && npm run db:seed",
    "db:reset": "node scripts/prisma-cli.cjs db push --force-reset && node scripts/run-seed.cjs --reset",
    "build:css": "tailwindcss -i ./src/styles/main.css -o ./public/assets/app.css --minify",
    "build": "npm run build:css",
    "start": "npm run build:css && npm run start:server",
    "start:server": "cross-env NODE_ENV=production tsx server.js",
    "lint": "tsc --noEmit",
    "postinstall": "node -e \"const { execFileSync } = require('child_process'); const fs = require('fs'); const path = require('path'); const root = path.resolve(process.env.INIT_CWD || process.cwd()); const script = path.join(root, 'scripts', 'prisma-cli.cjs'); if (!fs.existsSync(script)) { console.log('[postinstall] Prisma runner not found, skipping generate:', script); process.exit(0); } execFileSync(process.execPath, [script, 'generate'], { cwd: root, stdio: 'inherit', env: process.env });\"",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.19.3",
    "@prisma/client": "^6.7.0",
    "autoprefixer": "^10.4.27",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.6.1",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "image-size": "^2.0.2",
    "multer": "^1.4.5-lts.2",
    "pg": "^8.16.3",
    "postcss": "^8",
    "prisma": "^6.19.3",
    "tailwindcss": "^3.4.1",
    "tsx": "^4.19.4",
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/ejs": "^3.1.5",
    "@types/express": "^5.0.3",
    "@types/multer": "^1.4.12",
    "@types/node": "^20",
    "@types/pg": "^8.15.6",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "eslint": "^8"
  },
  "prisma": {
    "seed": "node scripts/run-seed.cjs"
  }
}
